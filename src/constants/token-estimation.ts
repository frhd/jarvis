/**
 * Token estimation constants for LLM interactions
 */

/** Average number of characters per token (rough approximation) */
export const CHARS_PER_TOKEN = 4;

/** Average words per minute for reading speed calculations */
export const READING_WORDS_PER_MINUTE = 200;
